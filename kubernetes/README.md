# Kubernetes deployment

Deploy with the following command

```sh
kubectl apply -f kubernetes/
```

## minikube

If you're using minikube, get the IP address using

```sh
minikube service <service-name> --url
```
